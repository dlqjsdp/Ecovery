<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ecovery.mapper.MemberMapper">

    <!-- 회원 등록 -->
    <insert id="insertMember" parameterType="com.ecovery.domain.MemberVO">
        insert into member (
            email, password, nickname, role, created_at
        ) values (
            #{email}, #{password}, #{nickname}, #{role}, #{created_at}
        )
    </insert>

    <!-- 회원 정보 수정 -->
    <update id="updateMember" parameterType="com.ecovery.domain.MemberVO">
        update member
        set password = #{password},
            nickname = #{nickname}
        where member_id = #{memberId}
    </update>

    <!-- 회원 번호로 조회 -->
    <insert id="findByMemberId" parameterType="long"

</mapper>